/**
 * Widget footprint sizes.
 * Values match the LinkitItem CVA variants the design system was derived from.
 */
export type WidgetSize = 'xs' | 'sm' | 'md' | 'lg' | 'xl';

/**
 * Surface treatment for a widget. `surface` is the neutral card;
 * the rest map onto the design system's pastel accent families.
 */
export type WidgetTone = 'surface' | 'lilac' | 'sky' | 'mint' | 'blush' | 'butter' | 'peach';
